#pragma once

/*class IObserver {
public:
    virtual ~IObserver() {};
    virtual void Update(const int& _x_from_hero) = 0;
};



class Observer {
public:
    Observer(Creature& hero) : _hero(hero) {
        this->_hero.hp();
    }

private:
    int _x;
    int _y;
    Creature &_hero;
    //static int static_number_;
    //int number_;
};*/

//int Observer::static_number_ = 0;

